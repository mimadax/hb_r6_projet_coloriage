{% extends 'base.html.twig' %}

{% block title %}Détails du livre{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ book.title }}</h1>

        {% if book.imageFilename %}
            <div class="mb-4">
                <img src="{{ book.imageFilename }}" alt="{{ book.title }}">
            </div>
        {% endif %}

        <p><strong>Auteur :</strong> {{ book.author }}</p>
        <p><strong>Date de publication :</strong> {{ book.publishedDate|date('d/m/Y') }}</p>
        <p><strong>Description :</strong> {{ book.description }}</p>

        <!-- Bouton pour retour a la liste -->
        <a href="{{ path('book_list') }}" class="btn btn-secondary">Retour à la liste</a>

        <!-- Bouton pour modifier le livre -->
        <a href="{{ path('book_edit', {'id': book.id}) }}" class="btn btn-primary">Modifier</a>

    </div>
{% endblock %}
